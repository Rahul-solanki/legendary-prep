package main;

// Check whether given array contains duplicates in k-distance or not
public class DuplicatesInKDistance {

    public static void main(String[] args) {
        int arr[] = {1, 0, 0, 1, 2, 3};
        if (checkDuplicatesWithinK(arr, arr.length, 2)) {
            System.out.println("Given array contains duplicates");

        }
    }

    static boolean checkDuplicatesWithinK(int array[], int n, int k) {
        for(int i=0; i<n-1; i++)
        {
//            for(int j = )
        }
        return true;
    }
}
